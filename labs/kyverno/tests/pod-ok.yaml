apiVersion: v1
kind: Pod
metadata:
  name: pod-ok
  namespace: kyverno-lab
  labels:
    app: demo
    team: platform
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      ports:
        - containerPort: 80
