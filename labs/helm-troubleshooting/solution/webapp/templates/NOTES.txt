Webapp installata nel namespace {{ .Release.Namespace }}.

Per testare il Service:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "webapp.fullname" . }} 8080:{{ .Values.service.port }}
  curl http://localhost:8080

Verifica gli endpoints:

  kubectl -n {{ .Release.Namespace }} get endpoints {{ include "webapp.fullname" . }}
