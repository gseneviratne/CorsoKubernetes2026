apiVersion: v1
kind: Pod
metadata:
  name: web-node-selector
  namespace: scheduling-lab
spec:
  nodeSelector:
    disk: ssd
  containers:
    - name: web
      image: busybox:1.36.1
      command: ["sh", "-lc"]
      args:
        - "sleep 365d"
