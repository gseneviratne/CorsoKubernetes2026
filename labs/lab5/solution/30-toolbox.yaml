apiVersion: v1
kind: Pod
metadata:
  name: toolbox
  namespace: lab5
  labels:
    app: toolbox
spec:
  restartPolicy: Never
  containers:
  - name: toolbox
    image: busybox:1.36
    command:
    - sleep
    - "3600"
